<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ecms.mapper.LoginLogMapper">

    <insert id="insert" parameterType="org.example.ecms.entity.LoginLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO login_log (user_id, username, login_type, ip, status, message)
        VALUES (#{userId}, #{username}, #{loginType}, #{ip}, #{status}, #{message})
    </insert>

    <select id="selectAll" resultType="org.example.ecms.entity.LoginLog">
        SELECT id, user_id, username, login_type, ip, status, message, created_at
        FROM login_log
        ORDER BY created_at DESC
    </select>
</mapper>
