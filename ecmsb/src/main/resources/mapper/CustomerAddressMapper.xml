<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ecms.mapper.CustomerAddressMapper">
    <select id="selectByCustomerId" resultType="org.example.ecms.entity.CustomerAddress">
        SELECT id, customer_id, receiver_name, receiver_phone, province, city, district, detail,
               is_default, created_at, updated_at
        FROM customer_address
        WHERE customer_id = #{customerId}
        ORDER BY is_default DESC, id ASC
    </select>
</mapper>
