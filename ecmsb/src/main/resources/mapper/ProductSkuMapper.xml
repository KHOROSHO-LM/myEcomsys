<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ecms.mapper.ProductSkuMapper">
    <select id="selectByProductId" resultType="org.example.ecms.entity.ProductSku">
        SELECT id, product_id, specs, price, stock, image, created_at, updated_at
        FROM product_sku
        WHERE product_id = #{productId}
        ORDER BY id ASC
    </select>
</mapper>
