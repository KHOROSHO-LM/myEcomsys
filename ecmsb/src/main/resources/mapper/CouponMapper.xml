<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ecms.mapper.CouponMapper">
    <select id="selectAll" resultType="org.example.ecms.entity.Coupon">
        SELECT id, name, type, threshold, discount_value, stock, used_count, per_user_limit,
               expire_start, expire_end, status, created_at, updated_at
        FROM coupon
        ORDER BY created_at DESC
    </select>
</mapper>
