<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ecms.mapper.SysUserMapper">

    <select id="selectByUsername" resultType="org.example.ecms.entity.SysUser">
        SELECT id, username, password, nickname, avatar, status,
               last_login_time, last_login_ip, created_at, updated_at
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <update id="updateLastLogin">
        UPDATE sys_user
        SET last_login_time = #{lastLoginTime},
            last_login_ip   = #{lastLoginIp}
        WHERE id = #{id}
    </update>
</mapper>
