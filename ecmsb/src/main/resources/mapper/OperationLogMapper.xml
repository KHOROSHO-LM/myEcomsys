<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.ecms.mapper.OperationLogMapper">
    <select id="selectAll" resultType="org.example.ecms.entity.OperationLog">
        SELECT id, user_id, username, module, action, request_url, request_method, ip, cost_time, created_at
        FROM operation_log
        ORDER BY created_at DESC
        LIMIT 200
    </select>
</mapper>
